# 고용24 OPEN-API 인증키 신청 가이드

Korea Job Finder에서 **워크24(고용24)** 채용정보를 수집하려면 고용24 OPEN-API 인증키가
필요합니다. 이 문서는 신청 화면의 **각 항목을 무엇으로 채워야 하는지**, 그리고 발급받은
키를 앱에 어떻게 넣는지 정리한 것입니다.

신청 화면 바로가기:
https://www.work24.go.kr/cm/e/b/0310/insertOpenApiSvcApprView.do
(로그인 필요 · 고용24 → 고객센터 → OPEN-API → 서비스 소개 및 신청)

신청 현황 확인: 같은 메뉴의 **신청현황**

---

## ⚠️ 0. 신청 전에 반드시 읽어야 할 것

신청 화면 상단 안내문에 이렇게 적혀 있습니다:

> 개인회원으로 채용정보API 신청시 **채용행사, 공채속보, 공채기업정보 API만** 이용하실 수
> 있으며, **채용정보목록, 채용정보상세 API는 이용이 불가능합니다.**

**이 앱이 필요로 하는 것은 "채용정보목록" API 입니다.**

즉, 기관구분을 **`개인`으로 신청하면 이 앱의 워크24 수집 기능에는 쓸 수 없습니다.**
현재 화면에 `개인`이 선택되어 있다면 아래 표를 보고 다시 판단해 주세요.

| 선택 | 받을 수 있는 API | 이 앱에서 쓸 수 있나? | 필요 서류 |
|---|---|---|---|
| **개인** | 채용행사, 공채속보, 공채기업정보 | ❌ **불가** (채용정보목록 없음) | 없음 |
| 공공 / 학교 | 채용정보목록 포함 | ✅ 가능 | 기관 증빙 |
| 민간 | 채용정보목록 포함 | ✅ 가능 | 사업자등록증 등 |
| **민간(직업소개, 직업정보제공기관)** | 채용정보목록 포함 | ✅ 가능 | ① 서비스 계획서 ② 사업자등록증 ③ 직업정보제공사업 신고확인증 |

### 그래서 어떻게 해야 하나 — 3가지 선택지

**A안 — 기관으로 신청 (채용정보목록이 꼭 필요한 경우)**
사업자등록증이 있어야 하고, 직업정보제공사업 신고확인증까지 요구될 수 있습니다.
개인이 준비하기에는 부담이 큽니다.

**B안 — 개인으로 신청 (지금 화면 그대로)**
채용행사 / 공채속보 / 공채기업정보만 받게 됩니다. 이 앱의 현재 워크24 수집기는
채용정보목록을 쓰므로 **그대로는 동작하지 않습니다.**
다만 "공채속보" 등을 쓰도록 수집기를 새로 만들 수는 있습니다 (요청 주시면 작업 가능).

**C안 — 워크24 없이 그대로 쓰기 (추천, 지금 이미 동작 중)**
이 앱은 **인증키가 하나도 없어도** 아래 4개 사이트에서 실제 공고를 가져옵니다:

| 사이트 | 키 필요 여부 |
|---|---|
| 잡코리아 | 불필요 |
| 인크루트 | 불필요 |
| 잡플로이 (외국인 전문) | 불필요 |
| 원티드 | 불필요 |

`베트남어` 검색 시 약 70건, `외국인` 검색 시 약 107건이 키 없이 수집됩니다.
워크24는 "있으면 좋은" 추가 소스이지, 앱이 돌아가기 위한 필수 조건이 아닙니다.

---

## 1. 신청 화면 항목별 작성법

### 기관구분 *
위 0번 표를 참고해 선택합니다.
`민간(직업소개,직업정보제공기관)`을 선택하면 **오픈API 채용정보 서비스 신청서**
다운로드 버튼이 나타나며, 작성 후 첨부해야 합니다.

### 서비스명 *
> 복사해서 붙여넣기:

```
Korea Job Finder - 외국인 구직자 채용정보 통합검색 서비스
```

### 서비스설명 * (1,000 Byte 제한)

한글은 UTF-8에서 글자당 3바이트라 **한글 330자 정도**가 한계입니다.
아래 예시는 **실제로 재어 본 결과 UTF-8 기준 591 Byte**(243자)라 수정할 여유가 넉넉합니다.

> 복사해서 붙여넣기:

```
국내 거주 외국인 구직자를 위한 채용정보 통합검색 서비스입니다.

'베트남어', '외국인', '통역' 등의 키워드로 여러 채용사이트의 공개 채용정보를 한 번에 조회하여, 근무지역·고용형태·경력조건을 동일한 기준으로 표준화하고 중복 공고를 제거해 한 화면에 보여줍니다.

목록 정보만 표시하며 '원문 보기'를 누르면 원본 공고로 이동해, 실제 지원은 해당 사이트에서 이루어집니다. 출처를 항상 표기하고 수집 데이터를 재판매하지 않습니다.
```

> **주의 1 — 길이:** 내용을 늘릴 때는 한글 1자 = 3바이트로 계산하세요.
> 위 예시 기준 **409 Byte 여유**, 즉 한글 약 136자까지 더 넣을 수 있습니다.
>
> **주의 2 — 사실대로:** 실제 운영 계획과 다르게 쓰면 안 됩니다. 영리 목적으로
> 운영할 계획이라면 "재판매하지 않습니다" 같은 문장을 사실에 맞게 고쳐 주세요.
> 심사에서 사용목적 소명을 요구받을 수 있습니다.

### 신청부서명 *
개인이면 부서가 없으므로 아래 중 하나를 씁니다.

```
개인
```
또는 본인 이름 (예: `홍길동`)

### 홈페이지주소 *
**`localhost` 주소는 쓸 수 없습니다.** 실제로 열리는 주소가 필요합니다.

- 깃허브에 코드를 올렸다면: `https://github.com/사용자명/korea-job-finder`
- 블로그/노션 등 소개 페이지가 있다면 그 주소
- 아무것도 없다면 깃허브 저장소를 하나 만들어 그 주소를 쓰는 것이 가장 간단합니다

### 사용API *
이 앱을 위해서는 아래 항목을 체크합니다.

| 체크 | 항목 | 이유 |
|---|---|---|
| ✅ | **채용정보** | 이 앱의 핵심. 채용 공고 목록을 가져옵니다 |
| ⬜ | 공동코드 | 지역·직종 코드표. 지역 필터를 더 정확히 하려면 유용 |
| ⬜ | 직업정보 | 직업 설명 데이터. 지금은 쓰지 않음 |
| ⬜ | 그 외 훈련과정·학과정보·심리검사 등 | 이 앱과 무관 — 체크하지 마세요 |

> 필요 없는 API를 많이 체크하면 심사에서 사용목적을 소명해야 할 수 있습니다.
> **꼭 필요한 것만** 체크하세요.

### 첨부파일
- 전체 5MB 이하, **1개 파일만** 업로드 가능
- 기관으로 신청하는 경우: 신청서 + 사업자등록증 + 직업정보제공사업 신고확인증을
  **하나의 PDF로 합쳐서** 올려야 합니다 (파일이 1개만 가능하므로)

---

## 2. 발급받은 키를 앱에 넣기

인증키가 나오면 `backend\.env` 파일에 넣습니다. 파일이 없으면 새로 만드세요.

```bash
copy backend\.env.example backend\.env
```

`backend\.env` 안에 아래처럼 적습니다:

```
WORK24_API_KEY=여기에_발급받은_인증키_붙여넣기
```

그리고 백엔드를 다시 시작합니다 (`run-backend.bat` 창을 닫고 다시 실행).

### 잘 들어갔는지 확인

브라우저에서 http://127.0.0.1:8000/api/sources 를 열고 `work24` 항목을 봅니다.

| status | 뜻 | 해야 할 일 |
|---|---|---|
| `not_configured` | 키가 아직 없음 | `.env` 저장 후 백엔드 재시작했는지 확인 |
| `invalid_key` | 키가 틀림 | 인증키를 다시 복사 (앞뒤 공백 주의) |
| `rate_limited` | 하루 호출 한도 초과 | 다음 날까지 대기 |
| `connected` | **정상** | 완료 |

앱 화면에서도 `워크24` 칩의 아이콘 색으로 바로 확인할 수 있습니다.
(노란 열쇠 = 키 필요, 빨간 열쇠 = 키 오류, 체크 = 정상)

---

## 3. ⚠️ 키를 받은 뒤 코드 수정이 한 번 더 필요합니다

현재 앱의 워크24 수집기는 예전 워크넷 시절 주소를 쓰고 있습니다:

```
https://openapi.work.go.kr/opi/opi/opiaEmpInfoSrch/list.do
```

**이 주소는 지금 404를 돌려줍니다** (직접 확인함). 고용24로 개편되면서 API 주소가
바뀌었는데, 정확한 새 주소는 **인증키를 발급받아야 볼 수 있는 "활용가이드" 문서**에
들어 있습니다.

그래서 순서는 이렇습니다:

1. 인증키 신청 → 승인
2. 고용24에서 **채용정보 API 활용가이드(엑셀/PDF)** 다운로드
3. 그 문서에 적힌 **요청 URL과 파라미터 이름**을 알려주시면
   `backend/app/collectors/work24.py` 를 그에 맞게 고쳐 드립니다

키만 넣으면 바로 될 것처럼 말씀드리지 않는 이유가 이것입니다. 주소가 바뀌어 있어서
활용가이드 없이는 정확히 맞출 수 없습니다.

---

## 4. 자주 막히는 부분

**Q. 신청이 거절되면?**
화면 하단에 "사용목적 등에 따라 신청이 거부될 수 있으며, 공문을 요청드릴 수도 있습니다"
라고 안내되어 있습니다. 서비스설명을 구체적으로, 사실대로 쓰는 것이 가장 중요합니다.

**Q. 홈페이지가 없으면 신청 못 하나요?**
필수 항목이라 비워둘 수 없습니다. 깃허브 저장소 주소가 가장 현실적인 대안입니다.

**Q. 워크24 없이도 앱이 쓸 만한가요?**
네. 현재 키 없이 4개 사이트에서 실제 공고를 수집하고 있고, 그중 **잡플로이는 외국인
채용 전문 사이트**라 이 앱의 목적과 가장 잘 맞습니다.

**Q. 사람인(Saramin)도 같은 방식인가요?**
아닙니다. 사람인은 개인도 바로 발급받을 수 있습니다:
https://oapi.saramin.co.kr/ 에서 신청 후 `backend\.env` 에
`SARAMIN_API_KEY=...` 를 넣으면 됩니다. 사람인 수집기는 공식 문서대로 구현되어 있어
**키만 넣으면 코드 수정 없이 바로 동작합니다.**

---

## 5. 요약

| 하고 싶은 것 | 해야 할 일 |
|---|---|
| 지금 바로 쓰고 싶다 | 아무것도 안 해도 됨 — 4개 사이트가 이미 동작 |
| 사람인을 추가하고 싶다 | https://oapi.saramin.co.kr/ 에서 키 발급 → `.env` (코드 수정 불필요) |
| 워크24를 추가하고 싶다 | 기관으로 신청 필요 → 승인 → 활용가이드 확인 → 코드 수정 1회 |

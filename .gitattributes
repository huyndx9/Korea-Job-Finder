# Shell scripts must keep LF endings, otherwise bash on macOS/Linux fails with
# "bad interpreter: /usr/bin/env bash^M" after a checkout on Windows.
*.sh text eol=lf

# Windows batch files need CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf
